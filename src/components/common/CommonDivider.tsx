import React from 'react';
import {Box} from "@mui/material";

const CommonDivider = () => {
    return (
        <Box sx={{
            background: 'linear-gradient(to right, #FFFFFF0A, #FFFFFF33, #FFFFFF0A)',
            height: '1px',
            mt: 6
        }}/>
    );
};

export default CommonDivider;